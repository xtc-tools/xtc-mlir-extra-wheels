name: XTC MLIR extra tools wheels build

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - {os: ubuntu-24.04, pkg: tools, python: cp310}
          - {os: ubuntu-24.04, pkg: tools, python: cp311}
          - {os: ubuntu-24.04, pkg: tools, python: cp312}
          - {os: ubuntu-24.04, pkg: tools, python: cp313}
          - {os: ubuntu-24.04, pkg: tools, python: cp314}
          - {os: ubuntu-24.04, pkg: dev, python: cp310}
          - {os: ubuntu-24.04-arm, pkg: tools, python: cp310}
          - {os: ubuntu-24.04-arm, pkg: tools, python: cp311}
          - {os: ubuntu-24.04-arm, pkg: tools, python: cp312}
          - {os: ubuntu-24.04-arm, pkg: tools, python: cp313}
          - {os: ubuntu-24.04-arm, pkg: tools, python: cp314}
          - {os: ubuntu-24.04-arm, pkg: dev, python: cp310}
          - {os: macos-14, pkg: tools, python: cp310}
          - {os: macos-14, pkg: tools, python: cp311}
          - {os: macos-14, pkg: tools, python: cp312}
          - {os: macos-14, pkg: tools, python: cp313}
          - {os: macos-14, pkg: tools, python: cp314}
          - {os: macos-14, pkg: dev, python: cp310}
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: PIP Cache
        uses: actions/cache@v4
        with:
          path: ~/pip_cache
          key: ${{ runner.os }}-${{ runner.arch }}-pip-cache-${{ matrix.pkg }}-${{ matrix.python }}-${{ github.run_id }}
          restore-keys: |
            ${{ runner.os }}-${{ runner.arch }}-pip-cache-${{ matrix.pkg }}-${{ matrix.python }}
            ${{ runner.os }}-${{ runner.arch }}-pip-cache-${{ matrix.pkg }}
            ${{ runner.os }}-${{ runner.arch }}-pip-cache
      - name: CCache Cache
        uses: actions/cache@v4
        with:
          path: ~/ccache
          key: ${{ runner.os }}-${{ runner.arch }}-ccache-${{ matrix.pkg }}-${{ matrix.python }}-${{ github.run_id }}
          restore-keys: |
            ${{ runner.os }}-${{ runner.arch }}-ccache-${{ matrix.pkg }}-${{ matrix.python }}
            ${{ runner.os }}-${{ runner.arch }}-ccache-${{ matrix.pkg }}
            ${{ runner.os }}-${{ runner.arch }}-ccache
      - name: Checkout XTC MLIR
        run: |
          ./checkout-xtc-mlir.sh
      - name: Install macOS Brew Packages
        if: runner.os == 'macOS'
        run: |
          brew install ccache
      - name: Instrall cibuildwheel
        run: |
          python3 -m venv .venv
          source .venv/bin/activate
          pip install -r requirements.txt
      - name: Build Wheels
        run: |
          export CIBUILDWHEEL=$PWD/.venv/bin/cibuildwheel
          export BUILD_PIP_CACHE_DIR=$HOME/pip_cache
          export BUILD_CCACHE_DIR=$HOME/ccache
          export BUILD_FOR_PYTHON=${{ matrix.python }}
          ./build-wheels-${{ matrix.pkg }}.sh
      - name: Store distribution packages
        uses: actions/upload-artifact@v5
        with:
          name: python-package-xtc-mlir-extra-${{ matrix.pkg }}-${{ matrix.python }}-${{ runner.os }}-${{ runner.arch }}
          path: wheelhouse/

  publish-to-testpypi:
    needs: [build]
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - {os_arch: Linux-X64, pkg: tools, python: cp310}
          - {os_arch: Linux-X64, pkg: tools, python: cp311}
          - {os_arch: Linux-X64, pkg: tools, python: cp312}
          - {os_arch: Linux-X64, pkg: tools, python: cp313}
          - {os_arch: Linux-X64, pkg: tools, python: cp314}
          - {os_arch: Linux-X64, pkg: dev, python: cp310}
          - {os_arch: Linux-ARM64, pkg: tools, python: cp310}
          - {os_arch: Linux-ARM64, pkg: tools, python: cp311}
          - {os_arch: Linux-ARM64, pkg: tools, python: cp312}
          - {os_arch: Linux-ARM64, pkg: tools, python: cp313}
          - {os_arch: Linux-ARM64, pkg: tools, python: cp314}
          - {os_arch: Linux-ARM64, pkg: dev, python: cp310}
          - {os_arch: macOS-ARM64, pkg: tools, python: cp310}
          - {os_arch: macOS-ARM64, pkg: tools, python: cp311}
          - {os_arch: macOS-ARM64, pkg: tools, python: cp312}
          - {os_arch: macOS-ARM64, pkg: tools, python: cp313}
          - {os_arch: macOS-ARM64, pkg: tools, python: cp314}
          - {os_arch: macOS-ARM64, pkg: dev, python: cp310}
    environment:
      name: testpypi
      url: https://test.pypi.org/p/xtc-mlir-extra-${{ matrix.pkg }}
    permissions:
      id-token: write  # IMPORTANT: mandatory for trusted publishing
    steps:
    - name: Download all the dists
      uses: actions/download-artifact@v6
      with:
        name: python-package-xtc-mlir-extra-${{ matrix.pkg }}-${{ matrix.python }}-${{ matrix.os_arch }}
        path: dist/
    - name: Publish distribution to Test PyPI
      uses: pypa/gh-action-pypi-publish@release/v1
      with:
        repository-url: https://test.pypi.org/legacy/
        verbose: true
        skip-existing: true

  publish-to-pypi:
    if: startsWith(github.ref, 'refs/tags/mlir-extra-v')  # only publish to PyPI on tag pushes
    needs: [build]
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - {os_arch: Linux-X64, pkg: tools, python: cp310}
          - {os_arch: Linux-X64, pkg: tools, python: cp311}
          - {os_arch: Linux-X64, pkg: tools, python: cp312}
          - {os_arch: Linux-X64, pkg: tools, python: cp313}
          - {os_arch: Linux-X64, pkg: tools, python: cp314}
          - {os_arch: Linux-X64, pkg: dev, python: cp310}
          - {os_arch: Linux-ARM64, pkg: tools, python: cp310}
          - {os_arch: Linux-ARM64, pkg: tools, python: cp311}
          - {os_arch: Linux-ARM64, pkg: tools, python: cp312}
          - {os_arch: Linux-ARM64, pkg: tools, python: cp313}
          - {os_arch: Linux-ARM64, pkg: tools, python: cp314}
          - {os_arch: Linux-ARM64, pkg: dev, python: cp310}
          - {os_arch: macOS-ARM64, pkg: tools, python: cp310}
          - {os_arch: macOS-ARM64, pkg: tools, python: cp311}
          - {os_arch: macOS-ARM64, pkg: tools, python: cp312}
          - {os_arch: macOS-ARM64, pkg: tools, python: cp313}
          - {os_arch: macOS-ARM64, pkg: tools, python: cp314}
          - {os_arch: macOS-ARM64, pkg: dev, python: cp310}
    environment:
      name: pypi
      url: https://pypi.org/p/xtc-mlir-extra-${{ matrix.pkg }}
    permissions:
      id-token: write  # IMPORTANT: mandatory for trusted publishing
    steps:
    - name: Download all the dists
      uses: actions/download-artifact@v6
      with:
        name: python-package-xtc-mlir-extra-${{ matrix.pkg }}-${{ matrix.python }}-${{ matrix.os_arch }}
        path: dist/
    - name: Publish distribution to PyPI
      uses: pypa/gh-action-pypi-publish@release/v1
      with:
        verbose: true
